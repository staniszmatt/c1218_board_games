import React from 'react';

export default (props) => {
    return (
        <div>
            <div> Complete New Events </div>
            <button> Complete New Events </button>
        </div>

    );
}
